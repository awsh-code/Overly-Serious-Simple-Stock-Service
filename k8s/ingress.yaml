apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stock-service-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /stock
        pathType: Prefix
        backend:
          service:
            name: stock-service
            port:
              number: 80
